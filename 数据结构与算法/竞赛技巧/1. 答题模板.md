```c++
#include <bits/stdc++.h>
#define int long long
#using namespace std;

const int N = 1e6,            // 用来定义数组
	inf = 1e18,               // 无穷
	mod = 1234567891          // 哈希

void func(){
	
}

signed main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	int T = 1;
	while(T--){
		func()
	}
	return 0;
}

```